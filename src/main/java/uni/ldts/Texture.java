package uni.ldts;import uni.ldts.elements.Drawable;import javax.imageio.ImageIO;import java.awt.*;import java.awt.image.BufferedImage;import java.io.File;import java.io.IOException;public class Texture implements Drawable {    private final BufferedImage img;    /**     * A texture is a drawable composed of an image     * (use png, jpeg doesn't support transparency)     */    public Texture(BufferedImage img) { this.img = img; }    public Texture(String path) {        try {            this.img = ImageIO.read(new File(path));        } catch (IOException e) {            throw new RuntimeException(e);        }    }    @Override    public void draw(int x, int y, Graphics2D g) {        // draws the image into the screen        g.drawImage(img, x, y, null);    }}